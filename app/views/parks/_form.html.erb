<%= bootstrap_form_for(@park) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.collection_check_boxes :ranger_ids, @rangers, :id, :name %>
  <%= f.file_field :picture %>
  <%= f.submit "Submit" %>
<% end %>
